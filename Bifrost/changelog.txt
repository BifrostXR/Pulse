Changelog - v0.1.50-beta

- Solved driver not loading for some users
- Fixed tracker ID always displaying zero
- Tracker ID textbox now shows "ID" when blank
- A disconnected device will no longer display the last tracker ID
- 'Esc' can now also be used to save tracker ID
- Deleting a previously specified tracker ID reverts to controller detection
- A tracking ID of zero (HMD) is not allowed
- Fixed UI freezing when exiting
- Exiting UI no longer disables devices